{
  "name": "yela-room",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "rm -rf docs/* && npm run build && cp -r dist/* docs/"
  },
  "devDependencies": {
    "rollup-plugin-visualizer": "^6.0.4",
    "vite": "^7.1.7",
    "vite-plugin-compression": "^0.5.1"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "include-media": "^2.0.0",
    "sass": "^1.93.2",
    "three": "^0.180.0"
  }
}
